apiVersion: v1
kind: Service
metadata:
  name: basic-node-service
spec:
  selector:
    app: basic-node-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort  # You can also use ClusterIP or LoadBalancer if needed
